spring.application.name=ecommerce-Blue
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Heroku automáticamente provee JDBC_DATABASE_URL para PostgreSQL
# NO configures driver, url, username, password - Heroku lo hace automático